# ZM 工具箱开发规范

本文档是 ZM 工具箱的长期开发基线。新增功能、重构、修复和发布前检查都必须参考本规范。

规范中的“必须”表示不可省略；“建议”表示若不采用，需要说明原因并确保没有降低安全性、兼容性或性能。

## 1. 产品原则

1. 默认离线、本地优先，不为了增加功能而引入不必要的服务器或账号系统。
2. 界面保持朴素、清晰、轻量，优先使用 Flutter 原生组件和字体图标。
3. 每个工具独立成模块，工具之间不得直接依赖彼此的页面或内部状态。
4. 原有账本数据、TXT 格式和公开文件路径必须向后兼容。
5. 不收集与功能无关的数据，不在日志、路由或错误信息中暴露用户内容。
6. 新功能必须能解释其权限、数据去向、失败状态和设备兼容范围。

## 2. 开发流程

每次开发依次执行：

1. 完整阅读本文档和当前任务涉及的代码。
2. 确认需求范围、现有数据兼容性和所需权限。
3. 新页面或明显 UI 调整必须先提供设计图，用户确认后才能开发。
4. 按工具或功能模块实现，不把大量逻辑堆入 `main.dart` 或单一页面。
5. 为关键计算、数据格式、边界输入和兼容逻辑补充测试。
6. 依次执行格式检查、静态分析、自动测试和必要的正式构建。
7. 交付时说明改动、检查结果、产物位置和仍存在的限制。

未经用户确认，不得因为顺手优化而扩大功能范围、改变数据格式或增加权限。

## 3. 项目目录与职责

```text
lib/
├── app/                    # 工具箱外壳与全局状态
│   ├── controllers/        # 收藏、最近使用等应用级流程
│   ├── models/             # 工具定义等应用级模型
│   ├── pages/              # 首页、分类、收藏、设置
│   ├── services/           # 应用级本地偏好
│   └── widgets/            # 工具箱通用组件
├── core/                   # 主题、颜色和稳定的通用配置
├── tools/                  # 每个工具一个独立目录
│   ├── calculator/
│   ├── unit_converter/
│   ├── expense/            # 账本完整模块
│   │   ├── controllers/    # 账本业务流程
│   │   ├── models/         # 账本数据模型
│   │   ├── screens/        # 账本页面
│   │   ├── services/       # 本地存储和公共 TXT 同步
│   │   ├── utils/          # 账本日期工具
│   │   └── widgets/        # 账本专用组件
│   └── <new_tool>/
└── expense_storage.dart    # 账本旧导入路径的兼容出口
```

### 3.1 工具模块目录

每个新增工具和现有工具都必须建立并维护独立文件夹：

```text
lib/tools/<tool_name>/
├── <tool_name>_screen.dart       # 必需：页面入口
├── <tool_name>_controller.dart   # 有持续状态或复杂流程时使用
├── models/                       # 有独立数据结构时使用
├── services/                     # 有存储、文件或平台能力时使用
└── widgets/                      # 有两个以上可复用局部组件时使用
```

简单工具可以只有一个 `screen.dart`，但不得把它合并到其他工具目录。

模块边界必须遵守：

- 工具专用的页面、状态、模型、存储、计算函数和组件必须全部放在对应的 `lib/tools/<tool_name>/` 内。
- 重构现有工具时也必须遵守本规则，不得只为新工具拆分目录。
- 账本的全部实现必须保留在 `lib/tools/expense/` 内；不得重新在 `lib/controllers`、`lib/models`、`lib/screens`、`lib/services`、`lib/utils` 或 `lib/widgets` 建立账本代码。
- 跨工具复用且不包含具体工具业务的代码才允许放入 `lib/core/` 或 `lib/app/widgets/`。
- 为兼容历史公开导入路径而保留的薄导出文件可以位于 `lib/` 根目录，但只能转发导出，不得承载业务实现。

### 3.2 工具注册

- 所有工具只在 `lib/tools/tool_registry.dart` 注册一次。
- 工具 ID 使用稳定的小写蛇形命名，例如 `date_calculator`。
- 工具 ID 发布后不得随意修改，否则收藏和最近使用会失效。
- 首页、分类、收藏和搜索必须共享注册表，不维护重复工具清单。
- 工具入口只传递必要参数，不在路由名称或日志中携带用户输入。

## 4. UI 规范

设计基线：

- 主色：`AppColors.primary` 深绿色。
- 背景：低对比浅色背景；内容卡片为白色。
- 边框：使用 `AppColors.line`，避免重阴影。
- 图标：使用 Material 字体图标，不使用分类插画或图片图标。
- 圆角、间距、输入框样式统一从主题或通用组件获取。
- 不使用大面积渐变、复杂动画和高成本模糊效果。

导航规则：

- 主导航固定为“首页、分类、收藏、设置”四项。
- 不为单个工具增加新的底部主导航入口。
- 工具详情页使用顶部返回栏，不显示工具箱底部导航。
- 账本内部可以保留自己的“账本、统计”导航。

交互规则：

- 一个页面应有一个清晰主操作。
- 删除、清空、覆盖等操作必须确认或提供可恢复路径。
- 保存、复制、失败和权限拒绝必须提供明确反馈。
- 空状态需要说明下一步操作，不只显示空白页面。
- 输入错误应在输入附近显示，不用系统异常代替用户提示。

适配规则：

- 页面必须适配窄屏、短屏、横屏和系统大字体。
- 不假设固定屏幕高度；可能溢出的内容使用滚动布局。
- 网格在窄屏下仍应保留可点击尺寸和完整标题。
- 异步完成后更新界面前必须检查 `mounted` 或 `context.mounted`。

## 5. 状态与组件规范

- 页面临时输入保留在页面内部，不进入全局控制器。
- 收藏、最近使用等跨页面状态由 `ToolboxController` 管理。
- 复杂工具将计算和状态移入自己的 controller，页面只负责展示和交互。
- 不在 `build()` 中执行文件读写、网络请求或高成本计算。
- `TextEditingController`、动画控制器、订阅和计时器必须在 `dispose()` 中释放。
- 重复两次以上的相同 UI 应评估提取为工具内组件或 `app/widgets` 通用组件。
- 通用组件不得依赖某个具体工具的模型。

## 6. 本地存储规范

- 默认只持久化完成业务所必需的数据。
- 密码生成结果、二维码输入、文本工具内容等敏感临时值默认不保存。
- 工具箱偏好只保存合法工具 ID，并在读取时过滤未知或非法值。
- 配置写入应使用临时文件和替换流程，降低中断导致损坏的风险。
- 存储失败不能阻止用户打开不依赖存储的工具。
- 不把缓存目录描述为用户永久文件位置。
- 公开文件必须使用系统提供的安全存储能力，不申请“所有文件访问权限”。

## 7. 账本兼容规范

账本属于长期兼容模块，以下规则不得在普通重构中改变：

- Android 公共目录：`/storage/emulated/0/Download/bill/<年份>/`。
- 每个 TXT 每个日期占一行。
- 同一天多条记录使用英文逗号 `,` 分隔。
- 支出金额不带负号；收入金额带 `+`。
- 默认文件名：`某年某月消费.txt`，允许用户安全重命名。
- 每个 TXT 独立保存“日期开头”或“完整年月日开头”格式。
- 用户输入中的换行、冒号和逗号必须经过安全转换，不能破坏 TXT 结构。
- 文件名必须拒绝路径穿越、控制字符和系统保留名称。
- Android 原生层继续校验文件名、年份和规范路径，不能只信任 Dart 层。
- 不直接修改或删除已有内部账本数据来完成迁移；需要迁移时必须先备份并测试旧版本数据。

## 8. 离线、接口与依赖规范

- 新工具默认设计为完全离线。
- 引入网络接口前必须获得用户明确同意，并说明数据发送目标、费用、限流和离线行为。
- 第三方 API 密钥不得写入 Dart、Android 资源或 APK；有密钥的服务必须通过受控后端代理。
- 网络工具必须设置超时、响应大小限制、状态码检查、数据结构校验和离线提示。
- 新依赖必须检查维护状态、许可证、平台支持和包体积影响。
- 能用 Dart/Flutter 标准库安全实现时，不增加第三方依赖。
- 图片、PDF、音视频等较大依赖应按功能延迟加载或明确评估 APK 增量。

## 9. 权限与隐私规范

- 权限按工具按需申请，不得在首次启动时集中索取。
- 无权限时工具必须可安全退出，并说明缺少的能力。
- 相机、麦克风、位置、通知和公开存储权限必须在 UI 中解释用途。
- 不输出账目、密码、原始文本、二维码内容、文件内容或用户路径到日志。
- 错误提示只提供用户可处理的信息，不暴露系统堆栈、数据库或设备实现细节。
- 不实现隐藏录像、后台窃听、剪贴板收集等高隐私风险能力。

## 10. 输入与计算安全

- 数字使用 `int.tryParse` 或 `double.tryParse`，禁止直接假定输入合法。
- 为长度、数值、日期范围、文件大小和列表数量设置合理上限。
- 计算结果必须处理除零、非有限数、溢出和无效日期。
- 计算器不得使用 `eval`、脚本引擎或执行用户表达式代码。
- 随机密码和安全令牌使用 `Random.secure`，普通抽签可使用独立随机逻辑。
- 二维码、JSON、Base64 等转换必须捕获格式和容量错误。
- 文件操作使用受信任目录和规范化路径，不拼接未经校验的用户路径。

## 11. 性能规范

- 优先使用 `const` widget 和惰性列表。
- 长列表使用 `ListView.builder`，避免一次构建大量不可见项目。
- 不在滚动过程中频繁读写文件。
- 输入实时计算需要保持轻量；高成本处理使用防抖、后台 isolate 或显式按钮触发。
- 图片和文件处理设置尺寸上限，并及时释放字节数据和控制器。
- 动画应短、少、可中断，不影响低端 Android 设备操作。

## 12. 注释与命名

- 文件、类和方法使用清晰英文命名；用户界面使用简体中文。
- 注释解释“为什么”和安全边界，不重复代码字面含义。
- 公开类和关键业务函数使用 `///` 文档注释。
- 复杂格式、兼容分支、平台限制和异常降级必须写注释。
- 禁止遗留无说明的 `TODO`、调试日志和注释掉的大段旧代码。

## 13. 测试规范

每个新工具至少覆盖：

1. 一个正常输入和正确结果。
2. 空输入或非法输入。
3. 一个边界值或极端值。
4. 关键按钮或页面状态切换。

涉及本地存储时，还应覆盖：

- 数据写入与恢复。
- 损坏数据降级。
- 未知工具 ID 过滤。
- 写入失败不阻塞核心功能。

账本相关改动必须继续通过：

- 收入、支出和结余计算。
- 多 TXT 创建、切换和数据隔离。
- 自定义文件名安全校验。
- 收入 `+` 和支出无负号格式。
- 旧数据缺少新字段时的兼容恢复。

## 14. 开发后检查顺序

必须按以下顺序执行：

```powershell
dart format --output=none --set-exit-if-changed lib test
flutter analyze --no-pub
flutter test --no-pub
```

涉及 Android、依赖、权限、平台通道或发布功能时，再执行：

```powershell
flutter build apk --release --no-pub
```

正式包还应核对：

- 包名、版本号和应用名称。
- APK 签名验证结果。
- APK 文件大小、生成时间和 SHA-256。
- Android Manifest 没有新增无关权限。
- 签名文件、密码和 `key.properties` 没有进入 Git。

## 15. 完成标准

功能只有同时满足以下条件才算完成：

- 与用户确认的设计和功能范围一致。
- 新增和现有工具的实现均收拢在各自的独立模块目录，并已接入统一注册表。
- 正常、空、错误和不支持状态均可处理。
- 不破坏原账本和本地数据。
- 没有新增未说明权限或数据上传。
- 注释、README 或必要文档已更新。
- 格式、静态分析和自动测试通过。
- 涉及发布时，正式构建和签名验证通过。

## 16. 当前设计基线

- `design/multitool-ui-overview-square.png`：已确认的工具箱基础设计。
- `design/offline-tools-overview-v1.png`：新增离线工具后的主页面设计。
- `design/offline-tools-details-calculation-v1.png`：计时和计算工具详情。
- `design/offline-tools-details-productivity-v1.png`：随机、计数和文本工具详情。

开发 UI 时应先对照这些设计，若设计之间冲突，以用户最后确认的版本为准。
